# PLACEHOLDER — NOT ENCRYPTED. The Bootstrap workflow overwrites stringData with
# real values (from GitHub Secrets or generated), then runs `sops -e -i` to encrypt
# this file in place with the instance's own Age key (see .sops.yaml). Never commit
# real plaintext secret values here.
apiVersion: v1
kind: Secret
metadata:
  name: variables-cluster
  namespace: flux-system
stringData:
  dex_client_secret: REPLACE_ME
  flux_web_client_secret: REPLACE_ME
  github_app_client_secret: REPLACE_ME
  oauth2_proxy_cookie_secret: REPLACE_ME
  r2_access_key_id: REPLACE_ME
  r2_secret_access_key: REPLACE_ME
  alertmanager_webhook_url: https://example.com/REPLACE_ME
