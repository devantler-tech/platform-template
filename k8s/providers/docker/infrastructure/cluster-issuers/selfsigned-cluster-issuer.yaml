# Bootstrap ClusterIssuer used solely to issue the platform CA Certificate.
# Not used directly by workloads — they use the platform-ca ClusterIssuer.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
