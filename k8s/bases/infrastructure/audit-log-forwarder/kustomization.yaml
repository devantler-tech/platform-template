apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Hetzner/prod opt-in only. The local Talos configuration does not write the
# host audit file this DaemonSet tails.
resources:
  - helm-repository.yaml
  - helm-release.yaml
