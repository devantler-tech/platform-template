apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gateway-certificate
  namespace: kube-system
spec:
  secretName: gateway-tls
  dnsNames:
    - "${domain}"
    - "*.${domain}"
  issuerRef:
    group: ${issuer_group}
    kind: ${issuer_kind}
    name: ${issuer_name}
