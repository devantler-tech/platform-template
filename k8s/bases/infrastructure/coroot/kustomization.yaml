apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The operator chart in infrastructure-controllers owns the Coroot CRD. Keep
# this CR in the dependent infrastructure layer to avoid a first-apply CRD
# discovery deadlock.
resources:
  - coroot.yaml
